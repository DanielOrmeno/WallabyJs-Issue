import { Inject, Provide } from 'vue-inversify-decorator';
import { Container } from './container';
import { TYPES } from './types';

export {
    Container as HdContainer,
    Inject as HdInject,
    Provide as HdProvide,
    TYPES
};
